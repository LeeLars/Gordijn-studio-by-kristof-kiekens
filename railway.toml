[service]
name = "gordijn-studio-cms"
root = "."
start = "node cms/server.js"

[build]
builder = "railpack"
installCommand = "cd cms && npm install"

# Persistent volume voor data
[[mounts]]
source = "/data"
destination = "./data"
